## 2024-05-18 - Glass Input Focus States
**Learning:** Glass inputs using `focus:outline-none` lack clear keyboard focus indicators by default.
**Action:** Pair `focus:outline-none` with `focus-visible:ring-2 focus-visible:ring-gold/50` for inputs to maintain accessibility within the design system.
