## 2026-05-07 - Form Input Accessibility & Disabled Actions
**Learning:** Added `htmlFor`/`id` binding on visible labels and `aria-label` for placeholder-only inputs. Also added immediate visual feedback via disabled button states to guide user interaction when required fields are missing.
**Action:** Always ensure every input has a programmatic label, and disable form action buttons clearly until prerequisites are met.
