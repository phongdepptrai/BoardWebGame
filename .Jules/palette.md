## 2024-05-24 - Focus Indicators and Hidden Labels
**Learning:** Adding `focus-visible` with a matching theme color (e.g., `gold/50`) helps maintain keyboard accessibility when custom inputs remove the default outline, and placeholder-only inputs need an explicit `sr-only` label for screen readers.
**Action:** Always include visible focus states and proper label associations, even for stylistic custom inputs.
